/**
 * enum for Direction that will be used in the Image Iterator that specifies which way to iterate.
 */
public enum Direction{
    /**
     * vertically meaning it starts at the upper left goes down then starts to the right and goes down again.
     */
    VERTICAL,
    /**
     * similar to vertical except it iterates from right to left then top to bottom.
     */
    HORIZONTAL
}
